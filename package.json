{
  "name": "anton-creator-analytics",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "shared",
    "server",
    "web"
  ],
  "scripts": {
    "typecheck": "tsc --build --verbose",
    "test": "npm run test --workspace @anton/shared && npm run test --workspace @anton/server",
    "db:seed": "npm run db:seed --workspace @anton/server",
    "db:reset": "npm run db:reset --workspace @anton/server",
    "dev:api": "npm run dev --workspace @anton/server",
    "dev:web": "npm run dev --workspace @anton/web"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
